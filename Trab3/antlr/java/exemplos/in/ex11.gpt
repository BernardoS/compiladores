algoritmo ex11;

variaveis
    x : inteiro;
fim_variaveis

inicio
    x := soma(2);  // ERRO: no. de argumentos insuficiente.
    imprima(x);
fim

funcao soma(x: inteiro, y: inteiro) : inteiro
inicio
    retorne x + y;
fim
