algoritmo ex09;

variaveis
    x, fat : inteiro;
fim_variaveis

inicio
    imprima("Digite um numero:");
    x := leia();
    fat := fatorial(x); // ERRO: funcao fatorial nao declarada
    imprima("fatorial de ",x," eh igual a ",fat);
fim
